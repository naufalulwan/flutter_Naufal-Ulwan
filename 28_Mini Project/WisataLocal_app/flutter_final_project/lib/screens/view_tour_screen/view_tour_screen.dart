import 'package:flutter/material.dart';

class ViewTourScreen extends StatelessWidget {
  const ViewTourScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
